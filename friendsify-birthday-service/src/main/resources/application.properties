server.port=9005

email.host.url = http://${EMAIL_HOST:localhost}:${EMAIL_PORT:9002}/email
person.host.url = http://${PERSON_HOST:localhost}:${PERSON_PORT:9001}/persons
friend.host.url = http://${FRIENDS_HOST:localhost}:${FRIENDS_PORT:9003}/friends

birthday.reminder.rate.seconds = ${BIRTHDAY_RATE:600}
birthday.range.days = ${BIRTHDAY_RANGE:2}